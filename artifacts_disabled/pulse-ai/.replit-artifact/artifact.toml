kind = "web"
previewPath = "/"
title = "Pulse AI"
version = "1.0.0"
id = "artifacts/pulse-ai"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 3000

[services.development]
run = "cd /home/runner/workspace && npm install && npm run dev -- --hostname 0.0.0.0 --port 3000"

[services.env]
PORT = "3000"
